package com.github.kimppa.exercises.ex2;

public class Config {

	/**
	 * Checks if HTML tags are allowed in user messages
	 * 
	 * @return Returns true if HTML tags are allowed.
	 */
	public boolean allowHtml() {
		return false;
	}
}
